from Missions import Align
from Missions import Movement
def start():

    """
    object = 0;
    buoy()
def buoy():
    TakePicture()
    if(detectObjet()):
        object = object + 1
        Align.start(10,10)
        if(object > 1):
            if(getDistance() < 10) :
                touchBuoy()
                return 0;
        else :
            Movement.forward(2)
           return buoy();
    return 0;

    """